# Local config (API keys, server UUIDs)
config.yaml

# Build output
dist/
.cache/
/pelican-steam-updater
/pelican-steam-updater_*

# Runtime sidecar files (created next to config.yaml)
*.log
*.state.yaml
*.lock

# OS / editor
.DS_Store
.idea/
.vscode/
